items met string waarde expanden tot argument. 
heredoc? 
$expanden als argument

# cdoor een filenaam. 

hecks of files geopend kunnen worden 
# < : zou altijd gevolgd moeten worden 
make sure that the following structure is used: 
cmd -flag <args>
so flags and arguments should be added together where relevant.